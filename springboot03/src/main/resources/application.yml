server:
  port: 8080
  servlet:
    context-path: /springboot03
#      上下文路径

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/test?useSSL=false&useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
#      SQL数据源地址
mybatis:
  type-aliases-package: com.cq.pojo #  扫描实体类的路径
  mapper-locations: classpath:mybatis/*.xml # mapper映射路径

